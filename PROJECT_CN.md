# 宠物领养风险决策系统

## 一句话介绍

这是一个用数据驱动、降低高风险宠物领养错配的决策系统。

## 解决的核心问题

很多人做领养决策时更依赖情绪，而不是自己的真实生活方式与宠物需求是否匹配，最终导致高风险错配。

## 为什么这是一个 Data Analyst 项目

这个项目的重点不是做网页，也不是做可视化报表，而是把业务问题抽象成一个可分析、可判断、可解释的决策问题：

- 定义目标变量：`mismatch_risk`
- 设计关键特征：离家时间、活动水平、居住环境、宠物类型
- 用领域知识构建规则标签
- 找出影响错配风险的关键驱动因素
- 把分析结果转化成用户可以执行的行为建议

## 目标变量

`Mismatch risk`

- `high risk`
- `low risk`

项目重点是尽量减少 `false negatives`，也就是不要漏掉真正高风险的领养场景。

## 主要特征

- `hours_away`：离家时间
- `activity_level`：活动水平
- `housing_type`：居住环境
- `pet_type`：宠物类型
- `pet_energy_level`：宠物精力水平

其中最关键的数据洞察是：

时间可投入程度和宠物精力水平，是影响错配风险最强的两个因素。

## 模型方法

项目采用的是 `rule-based model`，而不是复杂黑盒模型。

原因是：

- 决策类项目更看重可解释性
- 用户需要知道“为什么风险高”
- 输出不只是预测结果，还要能引导行动

## 输出内容

系统输出三部分：

- 风险分数
- 风险解释
- 行为建议

例如：

> 你的错配风险较高，因为你离家时间长、活动水平低，但选择了高精力宠物。建议考虑低精力宠物，或者先调整日常时间安排。

## 数据来源

当前使用的是模拟数据（synthetic data）。

这些数据基于现实约束和宠物照护常识生成，再通过规则逻辑进行风险标注。

## 做了哪些分析

项目的核心分析不是“展示数据”，而是：

- 比较用户生活方式和宠物需求是否匹配
- 识别高风险组合
- 验证用户调整行为后，风险是否下降

## 验证方式

因为当前不是真实领养平台数据，所以使用的是情景模拟验证：

- 模拟不同用户与宠物组合
- 判断哪些组合属于高风险
- 调整用户行为后再次评分
- 比较调整前后的风险变化

## 项目价值

这个项目的价值不在于做出一个漂亮报表，而在于：

它能直接改善决策，帮助用户减少高风险领养错误。

## 和普通 DA 项目的区别

普通 DA 项目很多停留在：

- 报表
- 可视化
- 描述性分析

这个项目更进一步，做的是：

- 特征设计
- 风险建模
- 决策支持
- 行为引导

## 后续改进方向

如果继续完善，下一步会是：

- 使用真实领养数据
- 基于真实结果重新标注风险
- 训练预测模型替代纯规则模型
- 做更细的风险分层和建议系统

## 你可以怎么讲这个项目

面试里可以直接这样说：

> 我做了一个数据驱动的宠物领养风险决策系统，目标是减少高风险错配。项目核心是把用户的生活方式特征，比如离家时间、活动水平、居住环境，与宠物的需求特征进行对比，用可解释的规则模型判断错配风险，并输出风险解释和行为建议。这个项目体现了特征设计、风险建模和把数据洞察转化为行动的能力。
